{
  "name": "peopleid",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "One identity for the .dot suite: the app name the host derives it from, the mask that holds it, and the handle that names it.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "peerDependencies": {
    "@noble/hashes": "^1.7.1",
    "@parity/product-sdk": "^0.19.1",
    "@parity/product-sdk-descriptors": "^0.8.0",
    "@parity/product-sdk-host": "^0.14.1",
    "@parity/product-sdk-signer": "^0.11.1",
    "polkadot-api": "^2.2.1"
  }
}
