{
  "name": "ethonchain",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 5189",
    "build": "tsc --noEmit && vite build",
    "fetch": "node scripts/fetch-content.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
