{
  "name": "thebutton-contract",
  "version": "0.1.0",
  "private": true,
  "description": "TheButton PolkaVM contract. Kept in its own workspace so Hardhat's toolchain does not collide with the Vite frontend's.",
  "scripts": {
    "build": "hardhat compile",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@parity/hardhat-polkadot": "^0.3.0",
    "@polkadot-labs/hdkd": "^0.0.29",
    "@polkadot-labs/hdkd-helpers": "^0.0.31",
    "@types/node": "^22.15.3",
    "hardhat": "^2.26.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
